name: CI

on:
  push:
    branches: ["*"]
  pull_request:
    branches: ["*"]

permissions:
  contents: read

jobs:
  lint:
    name: Lint
    uses: ./.github/workflows/lint.yml
    permissions:
      contents: read
      pull-requests: write
  build:
    name: Build
    uses: ./.github/workflows/build.yml
    permissions:
      contents: read
